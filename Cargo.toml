[package]
name = "massively_parallel_project"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 2


[dependencies]
coffee = { version = "0.4", features = ["opengl", "debug"] }
glam = "0.23"